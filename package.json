{
  "name": "mapbox-layer-manager",
  "version": "1.0.0",
  "description": "A utility for managing Mapbox basemap layers",
  "main": "dist/mapboxLayerManager.js",
  "scripts": {
    "build": "babel src -d dist",
    "prepublishOnly": "npm run build"
  },
  "keywords": ["mapbox", "basemap", "switch", "layers"],
  "author": "MarpleK",
  "license": "MIT",
  "peerDependencies": {
    "mapbox-gl": "^2.0.0"
  },
  "devDependencies": {
    "@babel/cli": "^7.25.6",
    "@babel/core": "^7.25.2",
    "@babel/preset-env": "^7.25.4"
  }
}
